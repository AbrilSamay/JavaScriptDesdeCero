//Representan una variable lacual no tiene valor, o tiene un tipo de dato erroneo, o no tiene valor en lo absolute.


null // Esta vacio, asignado aproposito por el programador.

NaN // Not a number, este da cuando el resultado esperado es un numero, pero lanza algo mas.

let notANumber = "hola" * 3.5;
console.log (notANumber)

// No podemos llevar a cabo una multiplicacion entre un string y un number.